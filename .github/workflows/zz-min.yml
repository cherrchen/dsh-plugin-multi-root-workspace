name: min
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['.github/workflows/zz-min.yml']
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run the unit suite
        run: echo "pnpm test"
